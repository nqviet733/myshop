/*
* main file
*/
require(['jquery', 'angular', 'bootstrap'] , function ($, angular, appModule) {

    // using jQuery because it will run this even if DOM load already happened
    $(function () {
        angular.bootstrap(document);
    });

    "Secret": "9925f389cd0de425a1f47090eabb095b"
});